#Perform BFS to determine the shortest sequence of characters
#between two strings. Note that you can choose one of 26 characters.
#Each word in the sequence has to be present in the dictionary
import PQueue

class getSequence:

	def __init__(self,s,t,D):
		self.startString = s
		self.endString = t
		self.D = D[:]
		self.visited = []
		self.queue = myQ
		self.queue.enqueue(start)

	def bfs(self,start):
		while q not empty
			dequeue
			generate n different words with n changes in characters
			check if words are already visited, if not add them to queue
			if word found return else continue
					
